# Commits that changed formatting only, and should not own a line in
# `git blame`. GitHub reads this file automatically; locally, opt in once
# with:
#
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Add the rustfmt adoption commit's full SHA below after committing it
# (`git rev-parse HEAD`). Only ever list commits that changed NO
# semantics -- the whole value of this file is that its entries are safe
# to skip when hunting who wrote a line and why.

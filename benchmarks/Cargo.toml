[package]
name = "flodl-bench"
version = "0.0.1"
edition = "2024"
publish = false

[features]
default = []
gpu = ["flodl/gpu"]
cuda = ["gpu", "flodl/cuda"]
rocm = ["gpu", "flodl/rocm"]

[dependencies]
flodl = { path = "../flodl" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
